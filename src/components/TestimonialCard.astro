---
export interface Props {
  nombre: string;
  viaje: string;
  foto?: string;
  rating: number;
  texto: string;
}

const { nombre, viaje, foto = '👤', rating, texto } = Astro.props;
---

<div class="testimonial-card">
  <div class="flex items-center gap-3 mb-4">
    <div class="avatar">
      {typeof foto === 'string' && foto.length === 2 ? (
        <span class="text-2xl">{foto}</span>
      ) : (
        <img src={foto} alt={nombre} class="w-12 h-12 rounded-full object-cover" />
      )}
    </div>
    <div>
      <h4 class="font-bold text-slate-900">{nombre}</h4>
      <p class="text-sm text-slate-500">{viaje}</p>
    </div>
  </div>

  <div class="flex gap-1 mb-3">
    {Array.from({ length: 5 }).map((_, i) => (
      <span class={i < rating ? 'text-orange-500' : 'text-slate-300'}>★</span>
    ))}
  </div>

  <p class="text-slate-700 leading-relaxed text-sm italic">"{texto}"</p>
</div>

<style>
  @reference "../styles/globals.css";
  .testimonial-card {
    @apply bg-white p-6 rounded-xl border border-slate-200 shadow-sm hover:shadow-md transition-shadow duration-300;
  }

  .avatar {
    @apply w-12 h-12 rounded-full bg-gradient-to-br from-orange-100 to-orange-50 flex items-center justify-center flex-shrink-0;
  }
</style>

